an application is composed of multiple features, and each featue is composed of multiple use cases.
use case can communicate with Database, Messaging, AI/ML Service, and other services using ports.
ports are interfaces, and the implementation of ports are in the shell.